import os from "os";


// console.log(os.platform());  // Get the platform of the operating system

// console.log(os.arch());  // Get the architecture of the operating system
// console.log(os.cpus());  // Get the CPU information of the operating system 

// console.log(os.homedir());  // Get the home directory of the operating system   

// console.log(os.networkInterfaces()); // Get the network interfaces of the operating system

// console.log(os.freemem()); // Get the free memory of the operating 

console.log(os.totalmem()); // Get the total memory of the operating system