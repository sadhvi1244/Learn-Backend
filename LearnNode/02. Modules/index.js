import greet from "./greet.js"; // import the greet module
import { person1, person2, person3 } from "./person.js"; // import the person module

console.log(person1); // prints the variable person1 inside the module/directry
console.log(person2); // prints the variable person2 inside the module/directry
console.log(person3); // prints the variable person3 inside the module/directry


greet("Sadhvi"); // call the greet function with the argument "Sadhvi"
console.log(greet); // prints the function greet inside the module/directry
